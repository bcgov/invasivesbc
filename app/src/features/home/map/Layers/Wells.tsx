import { GeoJSONObject } from '@turf/turf';
import { GeoJSON, Popup, Marker, useMapEvents } from 'react-leaflet';

export const getWellProximities = (inputGeo: GeoJSONObject) => {
  let returnVal;

  return returnVal;
};

export const WellsNearSpecificActivity = (inputGeo: GeoJSONObject) => {
  let data = {};
  return <GeoJSON data={}></GeoJSON>;
};
