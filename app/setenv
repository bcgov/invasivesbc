#/bin/bash
case $1 in
#todo:  print what the urls are going to be

  "dev")
    export REACT_APP_REAL_NODE_ENV='development'

    ;;

  "local")
    export REACT_APP_REAL_NODE_ENV='local'
    # TODO prompt user for optional port or use defaults
    ;;

  "test")
    export REACT_APP_REAL_NODE_ENV='test'
    ;;

  "prod")
    export REACT_APP_REAL_NODE_ENV='production'
    ;;
esac
